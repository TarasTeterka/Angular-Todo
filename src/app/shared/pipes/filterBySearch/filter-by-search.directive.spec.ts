import { FilterBySearchDirective } from './filter-by-search.directive';

describe('FilterBySearchDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterBySearchDirective();
    expect(directive).toBeTruthy();
  });
});
