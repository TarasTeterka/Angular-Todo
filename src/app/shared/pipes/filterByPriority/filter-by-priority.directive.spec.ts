import { FilterByPriorityDirective } from './filter-by-priority.directive';

describe('FilterByPriorityDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterByPriorityDirective();
    expect(directive).toBeTruthy();
  });
});
